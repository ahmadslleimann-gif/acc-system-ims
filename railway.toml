# Root-level Railway config: build the backend via the root Dockerfile.
# This works with the service Root Directory left at the repo root (default).

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
